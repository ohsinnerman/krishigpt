# KrishiGPT backend dependencies
# NOTE: versions relaxed from MASTER_PROMPT pins to be compatible with Python 3.13 on Windows.
# The behavior (not the exact version) is what the spec requires.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.1

# RAG / embeddings / vector store
google-genai>=1.0.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# Document ingestion
PyMuPDF>=1.24.0
langdetect>=1.0.9

# Infra / integrations
redis>=5.0.4
twilio>=9.1.0
httpx>=0.27.0
slowapi>=0.1.9

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.7

# Optional (Postgres persistence, eval) — kept light for the 14h build
# sqlalchemy>=2.0.30
# asyncpg>=0.29.0
# pandas>=2.2.0
